# 🌐 Настройка кастомного домена vkbouwmaster.com на Render.com

## Шаг 1: Добавление домена в Render

1. **Откройте Render Dashboard:**
   - Перейдите на https://dashboard.render.com
   - Выберите ваш сервис `vk-bouwmaster`

2. **Перейдите в Settings:**
   - В левом меню нажмите "⚙️ Settings"
   - Прокрутите вниз до раздела "Custom Domains"

3. **Добавьте домен:**
   - Нажмите "Add Custom Domain"
   - Введите: `vkbouwmaster.com`
   - Нажмите "Save"

4. **Добавьте www поддомен:**
   - Снова нажмите "Add Custom Domain"
   - Введите: `www.vkbouwmaster.com`
   - Нажмите "Save"

Render покажет вам DNS записи, которые нужно настроить.

## Шаг 2: Настройка DNS записей

### Вариант A: Если домен управляется через Cloudflare

1. **Откройте Cloudflare Dashboard:**
   - Перейдите на https://dash.cloudflare.com
   - Выберите домен `vkbouwmaster.com`

2. **Перейдите в DNS:**
   - В левом меню выберите "DNS" → "Records"

3. **Добавьте CNAME запись для основного домена:**
   - Нажмите "Add record"
   - **Type:** `CNAME`
   - **Name:** `@` (или оставьте пустым для корневого домена)
   - **Target:** `vk-bouwmaster.onrender.com`
   - **Proxy status:** 🟠 Proxied (оранжевое облако)
   - Нажмите "Save"

4. **Добавьте CNAME запись для www:**
   - Нажмите "Add record"
   - **Type:** `CNAME`
   - **Name:** `www`
   - **Target:** `vk-bouwmaster.onrender.com`
   - **Proxy status:** 🟠 Proxied (оранжевое облако)
   - Нажмите "Save"

⚠️ **Важно:** Если Cloudflare не позволяет использовать CNAME для корневого домена (@), используйте вместо этого:
- **Type:** `A` или `AAAA`
- **Target:** IP адрес, который предоставит Render (если доступен)
- Или используйте Cloudflare Workers/Pages для редиректа

### Вариант B: Если домен управляется через другого регистратора

1. **Откройте панель управления доменом** (где вы регистрировали домен)

2. **Добавьте CNAME запись:**
   - **Type:** `CNAME`
   - **Host/Name:** `www`
   - **Target/Value:** `vk-bouwmaster.onrender.com`
   - **TTL:** `3600` (или Auto)

3. **Для корневого домена (@):**
   - Некоторые регистраторы не поддерживают CNAME для корневого домена
   - В этом случае используйте A-запись с IP адресом, который предоставит Render
   - Или настройте редирект с `vkbouwmaster.com` на `www.vkbouwmaster.com`

## Шаг 3: Ожидание распространения DNS

После настройки DNS записей:

1. **Подождите 5-60 минут** для распространения DNS изменений
2. **Проверьте статус в Render:**
   - В Settings → Custom Domains
   - Должен появиться статус "Valid" рядом с доменом
   - Если статус "Pending", подождите ещё немного

## Шаг 4: Проверка SSL сертификата

Render автоматически выдаёт SSL сертификат через Let's Encrypt:

1. **Дождитесь активации сертификата:**
   - Обычно это занимает 5-15 минут после настройки DNS
   - Статус изменится на "Valid" в Render Dashboard

2. **Проверьте доступность:**
   - Откройте https://vkbouwmaster.com
   - Откройте https://www.vkbouwmaster.com
   - Оба должны работать с SSL (зелёный замочек в браузере)

## Шаг 5: Обновление конфигурации (если нужно)

Если вы хотите использовать домен в коде, убедитесь, что в `src/app/layout.tsx` указан правильный URL:

```typescript
metadataBase: new URL('https://vkbouwmaster.com'),
```

Это уже настроено в вашем проекте.

## Устранение проблем

### Домен не работает после настройки

1. **Проверьте DNS записи:**
   ```bash
   # Проверка CNAME записи
   dig www.vkbouwmaster.com CNAME
   
   # Или используйте онлайн инструменты:
   # https://dnschecker.org
   ```

2. **Проверьте статус в Render:**
   - Settings → Custom Domains
   - Если статус "Invalid", проверьте DNS записи

3. **Очистите DNS кэш:**
   ```bash
   # На macOS/Linux
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   ```

### SSL сертификат не выдаётся

1. **Убедитесь, что DNS записи настроены правильно**
2. **Проверьте, что домен указывает на Render:**
   - `dig vkbouwmaster.com` должен показывать IP Render
3. **Подождите до 24 часов** - иногда Let's Encrypt требует времени
4. **Свяжитесь с поддержкой Render**, если проблема не решается

### Редирект с www на без www (или наоборот)

Если вы хотите, чтобы один домен редиректил на другой:

1. **В Cloudflare:**
   - Используйте Page Rules или Workers для редиректа
   - Или настройте редирект на уровне Render (если доступно)

2. **В коде Next.js:**
   - Можно добавить middleware для редиректа (см. `middleware.ts`)

## Проверка работоспособности

После настройки проверьте:

- ✅ https://vkbouwmaster.com открывается
- ✅ https://www.vkbouwmaster.com открывается
- ✅ SSL сертификат активен (зелёный замочек)
- ✅ Все страницы работают корректно
- ✅ Загрузка файлов работает
- ✅ Админ-панель доступна

## Дополнительная информация

- **Render автоматически обновляет SSL сертификаты** - никаких действий не требуется
- **DNS изменения могут занять до 48 часов**, но обычно работают в течение часа
- **Бесплатный план Render** поддерживает кастомные домены без ограничений

