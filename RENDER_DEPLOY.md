# Инструкция по деплою на Render.com

## Быстрый старт

### Вариант 1: Через Blueprint (автоматически)

1. **Закоммитьте и запушьте изменения:**
   ```bash
   git add render.yaml package.json
   git commit -m "feat: add Render.com deployment configuration"
   git push origin main
   ```

2. **В Render Dashboard:**
   - Перейдите на https://dashboard.render.com
   - Нажмите "New +" → "Blueprint"
   - Выберите ваш репозиторий `vk-bouwmaster`
   - Render автоматически обнаружит `render.yaml` и создаст сервис

3. **Переменные окружения:**
   - Все необходимые переменные (`NODE_ENV`) уже настроены в `render.yaml`
   - Дополнительные переменные не требуются

4. **Дождитесь деплоя** - Render автоматически соберёт и запустит проект

### Вариант 2: Через UI (вручную)

1. **В Render Dashboard:**
   - Нажмите "New +" → "Web Service"
   - Подключите GitHub репозиторий `vk-bouwmaster`
   - Выберите ветку `main`

2. **Настройки сервиса:**
   - **Name:** `vk-bouwmaster`
   - **Environment:** `Node`
   - **Region:** `Frankfurt` (или ближайший к вам)
   - **Branch:** `main`
   - **Root Directory:** (оставьте пустым)
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm run start`
   - **Instance Type:** `Starter` (бесплатно) или `Standard` (платно, но без "засыпания")

3. **Переменные окружения:**
   В разделе "Environment Variables" добавьте:
   
   | Key | Value |
   |-----|-------|
   | `NODE_ENV` | `production` |

4. **Создайте сервис** - Render начнёт сборку

## Проверка деплоя

После успешного деплоя:

1. **Откройте URL:** `https://vk-bouwmaster.onrender.com`
2. **Проверьте админ-панель:** `https://vk-bouwmaster.onrender.com/admin`
3. **Попробуйте загрузить работу** - файлы должны сохраняться локально в `public/uploads`

## Важные замечания

### ⚠️ Бесплатный план (Starter)
- Сервис "засыпает" после 15 минут простоя
- Первый запрос после пробуждения может занять 1-2 секунды (cold start)
- Для production лучше использовать платный план `Standard`

### 💾 Хранение файлов

Проект использует локальное хранилище файлов:
- **Файлы:** сохраняются в `public/uploads/`
- **Данные:** сохраняются в `src/lib/works-data.json` и других JSON файлах

⚠️ **Важно:** На бесплатном плане Render файлы могут не сохраняться между перезапусками. Для production рекомендуется использовать платный план или внешнее хранилище (S3, Cloudflare R2).

### 🌐 Настройка кастомного домена

1. В Render → Settings → Custom Domains
2. Добавьте ваш домен (например, `vkbouwmaster.com`)
3. Настройте DNS записи:
   - **Type:** `CNAME`
   - **Name:** `@` или `www`
   - **Value:** `vk-bouwmaster.onrender.com`

## Устранение проблем

### Ошибка "Cannot find module '@vercel/blob'"
- Убедитесь, что `@vercel/blob` есть в `dependencies` в `package.json`
- Если нет, добавьте: `npm install @vercel/blob --save`

### Ошибка "Ошибка при загрузке файла"
- Проверьте логи Render → Logs на наличие ошибок API
- Убедитесь, что директория `public/uploads` может быть создана (права на запись)

### Сайт не отвечает
- Проверьте статус сервиса в Render Dashboard
- Если используется Starter план, подождите 1-2 секунды (cold start)
- Проверьте логи на наличие ошибок

## Мониторинг

- **Логи:** Render Dashboard → ваш сервис → Logs
- **Метрики:** Render Dashboard → ваш сервис → Metrics
- **События:** Render Dashboard → ваш сервис → Events

## Автоматический деплой

При использовании `render.yaml` с `autoDeploy: true`, каждый push в ветку `main` автоматически запускает новый деплой.

Чтобы отключить авто-деплой, измените в `render.yaml`:
```yaml
autoDeploy: false
```

