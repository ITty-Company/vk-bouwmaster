# 💾 Решение проблемы с загрузкой файлов на Render.com

## Проблема

На Render.com файловая система может быть **read-only** (только для чтения), поэтому файлы не сохраняются в `public/uploads/`.

## Решения

### Вариант 1: Render Disk (Рекомендуется для Render)

1. **В Render Dashboard:**
   - Откройте ваш сервис `vk-bouwmaster`
   - Перейдите в **Settings** → **Disk**
   - Нажмите **"Create Disk"**
   - Назовите его `uploads-disk`
   - Выберите размер (минимум 1 GB)
   - Mount path: `/uploads`

2. **Обновите код:**
   - Файлы будут сохраняться в `/uploads` вместо `public/uploads`
   - Нужно обновить путь в `src/app/api/upload/route.ts`

### Вариант 2: Внешнее хранилище (S3, Cloudflare R2)

Используйте облачное хранилище для файлов:
- **AWS S3**
- **Cloudflare R2** (бесплатно до 10 GB)
- **DigitalOcean Spaces**

### Вариант 3: Вернуть Vercel Blob Storage

Если у вас уже настроен Vercel Blob Storage, можно вернуть его использование.

## Быстрое решение (временное)

Для тестирования можно использовать **base64** кодирование (не рекомендуется для production, так как увеличивает размер данных).

## Проверка проблемы

После деплоя:

1. **Откройте консоль браузера** (F12 или Cmd+Option+I)
2. **Попробуйте загрузить файл** в админ-панели
3. **Проверьте ошибки в консоли:**
   - Если видите "Файловая система доступна только для чтения" → нужен Render Disk
   - Если видите другую ошибку → пришлите текст ошибки

## Логи на Render

Проверьте логи сервера:
- Render Dashboard → ваш сервис → **Logs**
- Ищите сообщения об ошибках загрузки файлов

## Рекомендация

Для production лучше использовать **Render Disk** или **внешнее хранилище** (S3/R2), так как:
- Файлы сохраняются надёжно
- Не теряются при перезапуске
- Масштабируемо

